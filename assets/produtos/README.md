# Imagens de produtos

Padrão visual do catálogo: proporção 4:5. As imagens podem ser associadas ao produto ou a uma variação específica por `product_media` no Supabase. Quando uma variação ainda não tiver imagem, o site usa um placeholder explícito em vez de reutilizar uma imagem incorreta.
