import { supabase } from './croma-supabase.js';

const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const brl=v=>v==null||v===''?'—':Number(v).toLocaleString('pt-BR',{style:'currency',currency:'BRL'});
const statusLabel=s=>s==='ok'?'Validado':s==='reject'?'Rejeitado':'Revisar';
const statusClass=s=>s==='ok'?'ok':s==='reject'?'bad':'warn';
const fmt=d=>d?new Date(d).toLocaleString('pt-BR'):'—';

function injectStyle(){if(document.querySelector('#supplierValidationStyle'))return;const s=document.createElement('style');s.id='supplierValidationStyle';s.textContent=`.sv-card{margin:16px 0}.sv-head{display:flex;justify-content:space-between;gap:12px;align-items:center;flex-wrap:wrap}.sv-counts{display:flex;gap:8px;flex-wrap:wrap}.sv-count{border:1px solid var(--croma-line);background:#fff;border-radius:12px;padding:9px 12px;font-weight:900;color:var(--croma-deep);cursor:pointer}.sv-count b{margin-left:6px}.sv-bulk{display:flex;gap:8px;flex-wrap:wrap;align-items:center;margin-top:12px}.sv-check{width:17px;height:17px}.sv-pending{font-size:.74rem;color:#8a6500;font-weight:800;margin-top:3px}.sv-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:10px}.sv-price{border:1px solid var(--croma-line);border-radius:12px;padding:12px}.sv-price small{display:block;color:var(--croma-muted)}.sv-price strong{display:block;color:var(--croma-deep);font-size:1.2rem;margin-top:4px}.sv-form{display:grid;grid-template-columns:220px 1fr;gap:12px;margin-top:14px}.sv-form textarea{min-height:76px}.sv-events{display:grid;gap:8px;margin-top:14px}.sv-event{border-left:3px solid #dcd9ee;padding:7px 0 7px 12px}.sv-event strong{display:block;color:var(--croma-deep)}.sv-event small{color:var(--croma-muted)}@media(max-width:800px){.sv-grid{grid-template-columns:1fr 1fr}.sv-form{grid-template-columns:1fr}}@media(max-width:520px){.sv-grid{grid-template-columns:1fr}}`;document.head.appendChild(s)}

async function invoke(payload){const{data:s}=await supabase.auth.getSession();const token=s?.session?.access_token;if(!token)throw new Error('Sessão expirada.');const{data,error}=await supabase.functions.invoke('croma-supplier-validation',{body:payload,headers:{Authorization:`Bearer ${token}`}});if(error)throw error;if(!data?.ok&&payload.action!=='bulk_set_status')throw new Error(data?.error||'Falha na validação.');return data}

async function countStatus(supplierId,status){const{count,error}=await supabase.from('supplier_catalog_items').select('id',{count:'exact',head:true}).eq('supplier_id',supplierId).eq('active',true).eq('validation_status',status);if(error)throw error;return count||0}

async function initCatalog(){
  const supplierId=new URLSearchParams(location.search).get('supplier');if(!supplierId||!document.querySelector('#catalogRows'))return;
  injectStyle();
  const anchor=document.querySelector('.supplier-kpis');const card=document.createElement('section');card.className='supplier-card sv-card';card.innerHTML=`<div class="sv-head"><div><h3 style="margin:0;color:var(--croma-deep)">Fila de validação</h3><div class="muted">Itens fora de “Validado” ficam sob consulta e não atualizam o custo operacional.</div></div><div class="sv-counts"><button class="sv-count" data-status="ok">Validado <b id="svOk">—</b></button><button class="sv-count" data-status="review">Revisar <b id="svReview">—</b></button><button class="sv-count" data-status="reject">Rejeitado <b id="svReject">—</b></button></div></div><div class="sv-bulk"><label class="supplier-toggle"><input id="svSelectVisible" type="checkbox"> Selecionar visíveis</label><span class="muted" id="svSelected">0 selecionado(s)</span><button class="btn light" id="svBulkOk">Validar</button><button class="btn light" id="svBulkReview">Revisar</button><button class="btn bad" id="svBulkReject">Rejeitar</button></div><p class="status" id="svStatus"></p>`;anchor?.after(card);
  const [ok,review,reject]=await Promise.all(['ok','review','reject'].map(s=>countStatus(supplierId,s)));document.querySelector('#svOk').textContent=ok.toLocaleString('pt-BR');document.querySelector('#svReview').textContent=review.toLocaleString('pt-BR');document.querySelector('#svReject').textContent=reject.toLocaleString('pt-BR');
  card.querySelectorAll('[data-status]').forEach(b=>b.onclick=()=>{const f=document.querySelector('#validationFilter');if(f){f.value=b.dataset.status;f.dispatchEvent(new Event('change',{bubbles:true}))}});
  const tbody=document.querySelector('#catalogRows');let decorating=false;
  async function decorate(){if(decorating)return;decorating=true;try{
    const table=tbody.closest('table'),head=table?.querySelector('thead tr');if(head&&!head.querySelector('.sv-col-head')){const th=document.createElement('th');th.className='sv-col-head';th.textContent='';head.prepend(th)}
    const trs=[...tbody.querySelectorAll('tr')].filter(tr=>tr.querySelector('a[href*="/interno/fornecedores/item/"]'));const ids=trs.map(tr=>{const a=tr.querySelector('a[href*="/interno/fornecedores/item/"]');return new URL(a.href).searchParams.get('item')}).filter(Boolean);if(!ids.length)return;
    const{data,error}=await supabase.from('supplier_catalog_items').select('id,validation_status,purchase_price,pending_purchase_price,validation_notes,validation_review_note').in('id',ids);if(error)throw error;const map=new Map((data||[]).map(x=>[x.id,x]));
    trs.forEach(tr=>{const a=tr.querySelector('a[href*="/interno/fornecedores/item/"]'),id=new URL(a.href).searchParams.get('item'),item=map.get(id);if(!item)return;if(!tr.querySelector('.sv-row-check')){const td=document.createElement('td');td.innerHTML=`<input class="sv-check sv-row-check" type="checkbox" data-id="${esc(id)}">`;tr.prepend(td);td.querySelector('input').onchange=updateSelected}const priceCell=tr.children[4];if(priceCell&&item.pending_purchase_price!=null&&!priceCell.querySelector('.sv-pending')){priceCell.insertAdjacentHTML('beforeend',`<div class="sv-pending">Proposto: ${brl(item.pending_purchase_price)}</div>`)}});updateSelected();
  }catch(e){console.error(e)}finally{decorating=false}}
  function selectedIds(){return[...tbody.querySelectorAll('.sv-row-check:checked')].map(x=>x.dataset.id)}
  function updateSelected(){document.querySelector('#svSelected').textContent=`${selectedIds().length} selecionado(s)`}
  const observer=new MutationObserver(()=>setTimeout(decorate,0));observer.observe(tbody,{childList:true,subtree:true});decorate();
  document.querySelector('#svSelectVisible').onchange=e=>{tbody.querySelectorAll('.sv-row-check').forEach(c=>c.checked=e.target.checked);updateSelected()};
  async function bulk(status){const ids=selectedIds();if(!ids.length)return alert('Selecione ao menos um item.');let note='';if(status!=='ok'){note=prompt(status==='reject'?'Informe o motivo da rejeição:':'Observação da revisão (opcional):','')??'';if(status==='reject'&&!note.trim())return}if(status==='ok'&&!confirm(`Validar ${ids.length} item(ns)? Quando houver preço proposto, ele passará a ser o preço aprovado.`))return;const box=document.querySelector('#svStatus');box.textContent='Processando validação…';box.className='status';try{const result=await invoke({action:'bulk_set_status',item_ids:ids,status,note});const failed=(result.results||[]).filter(x=>!x.ok);if(failed.length)throw new Error(`${failed.length} item(ns) não puderam ser processados.`);box.textContent='Validação concluída.';box.className='status ok';setTimeout(()=>location.reload(),500)}catch(e){box.textContent=e.message||'Falha na validação.';box.className='status bad'}}
  document.querySelector('#svBulkOk').onclick=()=>bulk('ok');document.querySelector('#svBulkReview').onclick=()=>bulk('review');document.querySelector('#svBulkReject').onclick=()=>bulk('reject');
}

async function initItem(){
  const itemId=new URLSearchParams(location.search).get('item');if(!itemId||!document.querySelector('#catalogDetails'))return;
  injectStyle();
  const{data:item,error}=await supabase.from('supplier_catalog_items').select('id,sku,name,purchase_price,pending_purchase_price,validation_status,validation_reasons,validation_notes,validation_review_note,validation_reviewed_at').eq('id',itemId).maybeSingle();if(error||!item)return;
  const{data:events}=await supabase.from('supplier_catalog_validation_events').select('id,event_type,from_status,to_status,previous_purchase_price,pending_purchase_price,approved_purchase_price,reasons,note,source,created_at,actor_id').eq('catalog_item_id',itemId).order('created_at',{ascending:false}).limit(30);
  const current=Number(item.purchase_price||0),pending=item.pending_purchase_price==null?null:Number(item.pending_purchase_price),delta=current>0&&pending!=null?((pending-current)/current)*100:null;
  const panel=document.createElement('section');panel.className='supplier-card detail-card sv-card';panel.innerHTML=`<div class="sv-head"><div><h2 style="margin:0 0 6px">Validação comercial</h2><div class="muted">Enquanto o status estiver fora de “Validado”, produtos vinculados ficam sob consulta e o novo custo não é aplicado.</div></div><span class="pill ${statusClass(item.validation_status)}">${statusLabel(item.validation_status)}</span></div><div class="sv-grid" style="margin-top:14px"><div class="sv-price"><small>Preço aprovado atual</small><strong>${brl(item.purchase_price)}</strong></div><div class="sv-price"><small>Preço proposto</small><strong>${brl(item.pending_purchase_price)}</strong></div><div class="sv-price"><small>Variação</small><strong>${delta==null?'—':`${delta>=0?'+':''}${delta.toFixed(1)}%`}</strong></div><div class="sv-price"><small>Última revisão</small><strong style="font-size:.9rem">${fmt(item.validation_reviewed_at)}</strong></div></div>${(item.validation_reasons||[]).length?`<div class="notice warn" style="margin-top:12px"><strong>Motivos da revisão</strong><div style="margin-top:6px">${(item.validation_reasons||[]).map(r=>`• ${esc(r.label||r.code)}${r.detail?` — ${esc(r.detail)}`:''}`).join('<br>')}</div></div>`:''}<div class="sv-form"><div class="field"><label for="svApprovedPrice">Preço a aprovar</label><input id="svApprovedPrice" type="number" min="0.01" step="0.01" value="${esc(pending??(current||''))}"></div><div class="field"><label for="svNote">Observação da validação</label><textarea id="svNote" placeholder="Ex.: conferido no site do fornecedor / tabela corrigida">${esc(item.validation_review_note||'')}</textarea></div></div><div class="sv-bulk"><button class="btn" id="svValidate">Validar e aplicar preço</button><button class="btn light" id="svKeepReview">Manter em revisão</button><button class="btn bad" id="svReject">Rejeitar</button></div><p class="status" id="svItemStatus"></p><div style="margin-top:18px"><h3 style="margin:0 0 8px;color:var(--croma-deep)">Histórico de validação</h3><div class="sv-events">${(events||[]).length?(events||[]).map(e=>`<div class="sv-event"><small>${fmt(e.created_at)}</small><strong>${statusLabel(e.to_status)}${e.source?` · ${esc(e.source)}`:''}</strong><div class="muted">${e.previous_purchase_price!=null?`Anterior ${brl(e.previous_purchase_price)} · `:''}${e.pending_purchase_price!=null?`Proposto ${brl(e.pending_purchase_price)} · `:''}${e.approved_purchase_price!=null?`Aprovado ${brl(e.approved_purchase_price)}`:''}${e.note?`<br>${esc(e.note)}`:''}</div></div>`).join(''):'<span class="muted">Sem eventos de validação.</span>'}</div></div>`;
  const main=document.querySelector('.detail-main');main?.prepend(panel);
  async function action(status){const note=document.querySelector('#svNote').value.trim(),price=Number(document.querySelector('#svApprovedPrice').value),box=document.querySelector('#svItemStatus');if(status==='reject'&&!note)return alert('Informe o motivo da rejeição.');if(status==='ok'&&(!Number.isFinite(price)||price<=0))return alert('Informe um preço maior que zero.');box.textContent='Salvando…';box.className='status';try{await invoke({action:'set_status',item_id:itemId,status,note,approved_price:status==='ok'?price:(Number.isFinite(price)&&price>=0?price:null)});box.textContent='Validação atualizada.';box.className='status ok';setTimeout(()=>location.reload(),500)}catch(e){box.textContent=e.message||'Falha na validação.';box.className='status bad'}}
  document.querySelector('#svValidate').onclick=()=>action('ok');document.querySelector('#svKeepReview').onclick=()=>action('review');document.querySelector('#svReject').onclick=()=>action('reject');
}

if(location.pathname.includes('/interno/fornecedores/catalogo'))initCatalog().catch(console.error);
if(location.pathname.includes('/interno/fornecedores/item'))initItem().catch(console.error);
