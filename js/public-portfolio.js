import { supabase } from './croma-supabase.js';
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
export async function renderPortfolio(root,featured=false){if(!root)return;try{let q=supabase.from('portfolio_items').select('id,title,description,image_url,image_alt,sort_order,featured,active,is_reference,image_source_type').eq('active',true).eq('is_reference',false).eq('image_source_type','propria').order('sort_order').order('title');if(featured)q=q.eq('featured',true).limit(4);const{data,error}=await q;if(error)throw error;const rows=data||[];root.innerHTML=rows.length?rows.map(x=>`<article class="cm-card"><figure><img src="${esc(x.image_url)}" alt="${esc(x.image_alt||x.title)}" loading="lazy" decoding="async"></figure><h3>${esc(x.title)}</h3>${x.description?`<p>${esc(x.description)}</p>`:''}</article>`).join(''):`<p class="cm-status">O acervo público está sendo atualizado. <a href="/contato/">Fale com a Croma sobre seu projeto.</a></p>`}catch(e){console.warn('Portfólio indisponível',e);root.innerHTML='<p class="cm-status">Não foi possível carregar os trabalhos agora. <a href="/contato/">Fale com a Croma.</a></p>'}}
if(document.querySelector('#publicPortfolio'))renderPortfolio(document.querySelector('#publicPortfolio'));
